# 页面设计规范

后续在本工作区生成或修改页面时，先阅读并遵循 `99-附件与参考/ui-design-theory-handbook.md`，将其作为 UI 设计、交互实现和验收的默认依据。用户明确指定的项目需求与视觉参考应结合该规范落实。

- 先明确用户目标、主动作、使用场景、成功标准与约束，再设计页面。
- 建立清晰的信息层级，突出主操作，使用用户熟悉的业务语言。
- 复用一致的颜色、字体、间距、圆角和阴影规则；间距优先收敛为 4、8、12、16、24、32 等 token。
- 为组件覆盖适用的聚焦、已选、禁用、加载、空、成功和错误状态；失败提供原因、恢复办法并保留用户输入。
- 保证文字对比度、键盘操作、可见焦点、图标名称与足够的点击区域；不只用颜色表达状态。
- 动效用于解释变化，支持减少动态效果，不能延迟核心任务。
- 不编造结果或指标；交付前按手册第 10 节的设计评审清单检查。

本规范是任务与可用性原则，不是固定视觉皮肤；具体品牌风格、布局和组件仍须符合当前项目要求。
